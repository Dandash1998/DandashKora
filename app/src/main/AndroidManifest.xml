<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <queries>
        <package android:name="com.emanuelef.remote_capture" />
    </queries>

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher_4_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_4_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DandashKora"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".BlockedActivity"
            android:exported="false" />

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3760228049158676~9588942680" />

        <activity
            android:name=".VideoPlayer.selectedmatch"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            android:usesCleartextTraffic="true" />

        <activity
            android:name=".splash.Splashactivity"
            android:exported="true"
            android:theme="@style/Theme.DandashKora">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.DandashKora" />

    </application>

</manifest>